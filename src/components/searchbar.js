import React from 'react';

export default class Searchbar extends React.Component{
    render(){
        return (
            <div>
                <input type="text"/>
                <button>Search</button>
            </div>
        );
    }
}